<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.miduchina.wrd.api.mapper.infomonitor.BriefMapper">

	<select id="getSourceMaterialCount" resultType="java.lang.Integer">
		<![CDATA[
			SELECT
				sum(analysis_num)
			FROM
				brief_info
			WHERE
				user_id = #{userId}
			AND click_type = 1
			AND is_edit = 0
			AND `status` = 1
			AND create_time BETWEEN #{startTime} and #{endTime}
		]]>
	</select>
	
</mapper>